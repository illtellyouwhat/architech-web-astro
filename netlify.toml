# Netlify configuration for Astro site with Forms

[build]
  publish = "dist"
  command = "npm run build"

# Form notifications
[[plugins]]
  package = "@netlify/plugin-form-notifications"

  [plugins.inputs]
    email = "hello@automationarchitech.com"
