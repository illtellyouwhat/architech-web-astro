

# START HERE - Automation Architech Project

---

1. Read this file completely
2. Read **ONLY the most recent entry** in `HISTORY-executive-timeline.md`
3. If date on `INVENTORY-content-[date].md` is >2 sessions old, request updated inventory

---

## CRITICAL RULES

1. **All colors/fonts** come from `LOCK-design-system.md` (Gray-900 primary, NOT blue)
2. **All copy** comes from `LOCK-content.md`  
3. **Tech stack decisions** come from `LOCK-architecture.md`
4. **Never invent content** - preserve existing or use explicit placeholders
5. **User controls git commits** - never mention committing
6. **Surface conflicts immediately** - if lock files contradict specs or each other, ask for clarification
7. **Always use actual current date** - Look up today's date, never assume or use stale dates

---

## FILE HIERARCHY

```
LOCK-* (immutable constraints - colors, copy, architecture)
    Ã¢â€ â€œ
SPEC-* (implementation instructions referencing locks)
    Ã¢â€ â€œ
RESOURCE-* (source materials - mockups, examples, references)
    Ã¢â€ â€œ
HISTORY-* (audit trail of completed work - always dated)
    Ã¢â€ â€œ
INVENTORY-content-[date].md (current state snapshot from Claude Code - always dated)
```

**When conflicts arise**: Stop and ask for disambiguation rather than guessing.

---

## HOW LOCK FILES WORK

Lock files show **desired current state**, not changelogs.

**Workflow:**
1. User and Claude UI discuss desired changes
2. Claude UI updates lock files to reflect agreed decisions
3. Claude creates `SPEC-[phase-name].md` referencing lock file: "Update to match LOCK-content.md line 42"
4. Spec passed to Claude Code with header prompt
5. Claude Code reads lock file for exact content, uses spec for implementation context

**Lock files updated BEFORE specs, not after.**

---

## FILE NAMING CONVENTIONS

- `LOCK-*` = Immutable constraints (design system, content, architecture)
- `HISTORY-*` = Audit trail (executive timeline with dates - append-only)
- `SPEC-*` = Implementation instructions (one per phase/feature)
- `PROMPT-*` = Reusable procedures for Claude Code
- `RESOURCE-*` = Source materials (mockups, case studies, references, screenshots)
- `INVENTORY-content-[date].md` = Current site state snapshot (generated by Claude Code, always dated)

---

## CONTENT INVENTORY SYSTEM

**Purpose**: Let Claude UI see actual current state of website

**Location**: `INVENTORY-content-[date].md` (flat in project root, always with date)

**When to request update**:
- If inventory date is >2 sessions old AND changes have been made
- If Claude UI needs to reference current site structure
- After major implementation phases complete

**How to generate**: Give Claude Code the prompt from `PROMPT-generate-content-inventory.md`

**Note**: Inventory lives in THIS project (web-based Claude), generated by Claude Code with current date

---

## CURRENT PROJECT STATE

**Read**: Most recent entry in `HISTORY-executive-timeline.md` (always check date)

**Check**: Date on `INVENTORY-content-[date].md` - if stale, request update

---

## WORKFLOW

1. **User** describes desired changes OR uploads mockup/resource
2. **Claude UI (me)** discusses and agrees on changes with user
3. **Claude UI (me)** updates relevant lock files to reflect decisions
4. **Claude UI (me)** creates `SPEC-[phase-name].md` referencing lock files
5. **User** passes spec + lock files + `PROMPT-claude-code-header.md` to Claude Code
6. **Claude Code** implements changes
7. **User** reviews, tests, commits manually
8. **User** requests HISTORY update (Claude UI adds entry to `HISTORY-executive-timeline.md`)
9. **(Optional)** Claude UI (me) requests content inventory update if needed for next session

---

## HISTORY FILE MANAGEMENT (USER-TRIGGERED ONLY)

**File**: `HISTORY-executive-timeline.md`

### When to Update:
- **ONLY when user explicitly requests it**
- User will say: "update the history file", "add to timeline", "document this session", etc.
- **Never update proactively** - wait for user to trigger

### Entry Format Requirements:

**Date**: ALWAYS use actual current date (lookup if needed, never assume)
**Chronological Order**: Most recent entry FIRST (newest at top)
**Phase**: Can have multiple phases in one day (Phase 6 + Phase 7, etc.)

### What to Include:

**"What We Did" Section:**
- Strategic decisions made (not just implementation)
- Phases planned and/or implemented
- Major discussions and conclusions
- Multiple phases on same day = list all

**"Files Updated" Section:**
- Track LOCK file changes (which lock files, what was added)
- Track SPEC file creation (SPEC-phase-X.md)
- Track new unexpected files (summaries, resources, etc.)
- Version number updates (LOCK-architecture.md v1.0 â†’ v2.0)

### Examples of Proper Entries:

```markdown
## December 9, 2024 - Phase 8: Dynamic Case Studies Architecture

**What We Did**:
- [Strategic decisions about Content Collections]
- [Multiple bullet points about implementation]

**Files Updated**:
- CONTENT-LOCK.md (added Case Studies section, lines 528-690)
- LOCK-architecture.md (v2.0 - added Content Collections Architecture, 240 new lines)
- SPEC-phase-8.md (created - implementation instructions)
- clinical-trial-patient-matching.md (created - case study markdown)

**Notes**:
- [Important context for future sessions]
```

### Date Ordering Rules:

âœ… **CORRECT ORDER (newest first):**
```
## December 9, 2024
## December 8, 2024  
## December 2, 2024
## December 1, 2024
```

âŒ **WRONG ORDER:**
```
## December 2, 2024  (Don't mix dates)
## December 8, 2024
## December 1, 2024
```

### Multiple Phases Same Day:

If working on Phase 6 AND Phase 7 on December 8:
```markdown
## December 8, 2024 - Phase 6 & 7: Homepage Updates + Case Studies Cleanup

**What We Did**:
- Phase 6: [homepage changes]
- Phase 7: [case studies changes]
```

OR separate entries with same date if distinct:
```markdown
## December 8, 2024 - Phase 7: Case Studies Cleanup
[details]

---

## December 8, 2024 - Phase 6: Homepage Content Updates  
[details]
```

### Update Checklist (When User Requests):

When user triggers HISTORY update:
1. âœ… Lookup: What is today's ACTUAL date?
2. âœ… Create: New entry with today's date
3. âœ… Insert: At TOP of file (newest first)
4. âœ… Verify: Chronological order is correct
5. âœ… Document: All lock file changes and new files
6. âœ… Return: Complete updated file to user in chat

**CRITICAL**: Never update HISTORY-executive-timeline.md without explicit user request.

---

## USER CONTEXT

- **Skills**: Photoshop expert, visual designer
- **Limitations**: Limited modern web dev experience
- **Preferences**: Strategic guidance > implementation details
- **Tools**: Claude UI (strategy), Claude Code (implementation)
- **Approach**: 80/20 principle - broad strokes first, polish later
- **Values**: Epistemic humility, challenge assumptions, fill gaps

---

## PROJECT PRINCIPLES

1. **Credibility before conversion** - Prove first, sell second
2. **Concrete metrics over abstractions** - "6-7x capacity" not "increased efficiency"  
3. **Proof-based messaging** - Real case studies with measurable outcomes
4. **Industry-specific terminology** - Use insider language
5. **Preserve don't invent** - Never create content unprompted

---

## COMMON PATTERNS

**User uploads mockup** Ã¢â€ â€™ Claude creates spec with file-by-file instructions
**User discusses content changes** Ã¢â€ â€™ Claude updates LOCK-content.md, then creates spec
**User discusses design changes** Ã¢â€ â€™ Claude updates LOCK-design-system.md, then creates spec
**Mid-project check-in** Ã¢â€ â€™ Claude reads last entry in HISTORY-executive-timeline.md (check date)
**Content inventory stale** Ã¢â€ â€™ Claude requests updated inventory from Claude Code

---

## CONFLICT RESOLUTION

**Priority order (highest to lowest):**
1. Explicit user instruction in current conversation
2. Lock files (LOCK-*)
3. Spec files (SPEC-*)
4. Resource files (RESOURCE-*)
5. History files (HISTORY-*)

**If any file contradicts another**: Surface the conflict immediately and ask for clarification. Never guess or assume intent.

---

## CLAUDE CODE INTEGRATION

**Header prompt**: `PROMPT-claude-code-header.md` (pass with every spec)

**Content inventory prompt**: `PROMPT-generate-content-inventory.md` (run when inventory needed, Claude Code will date it)